[project]
# Python project configuration
name = "mlc"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.7"

[tool.uv]
dependency-groups = {
    docs = ["sphinx", "sphinx_rtd_theme", "napoleon"]
}
